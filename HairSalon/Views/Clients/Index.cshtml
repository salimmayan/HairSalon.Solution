@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Client>;  //new

<h1>Clients</h1>

@if (@Model.Count == 0)
{
  <h3>No clients have been added yet!</h3>
} 

<ul>
  @foreach(Client client in Model)
  {
      <li>@Html.ActionLink($"{client.Description}", "Details", new { id = client.ClientId }) | @client.Stylist.Name</li>    @* new *@
      @* <li>@Html.ActionLink($"{item.Description}", "Details", new { id = item.ItemId })</li> *@
      @* <li>@Html.ActionLink($"{item.ItemId}", "Details")</li> *@
  }
</ul>

<p>@Html.ActionLink("Add new client", "Create")</p>

<p>@Html.ActionLink("Home", "Index", "Home")</p>