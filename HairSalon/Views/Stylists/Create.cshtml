@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<h2>Add a new Stylist</h2>


@using (Html.BeginForm())
{
  <div class="divClass">
    <span class="left">@Html.LabelFor(model => model.Name)</span>
    <span class="right">@Html.TextBoxFor(model => model.Name)</span>
    <br><br>
    <input type="submit" value="Add new stylist" class="btn btn-default" />
  </div>
}

<br> <br> <br>

<p>@Html.ActionLink("Show all stylists", "Index")</p>